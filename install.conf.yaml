- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.zshrc: .zshrc
    ~/.vimrc: .vimrc

- shell:
  - [touch ~/.zshrc.local, Creating zsh local file]
  - [git submodule update --init --recursive, Installing submodules]

