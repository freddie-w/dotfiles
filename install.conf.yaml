- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.zshrc: .zshrc
    ~/.vimrc: .vimrc

- create:
    - ~/.zshrc.local

- shell:
  - [git submodule update --init --recursive, Installing submodules]

